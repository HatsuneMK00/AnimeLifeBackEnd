on:
  workflow_dispatch:
  
jobs:
  set_up_go:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: ls
    - run: ls . -l
  
    - uses: actions/setup-go@v3
      with:
        go-version: 1.20
    - run: go version
      
